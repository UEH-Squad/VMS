@attribute [Route(Routes.VolunteersList + "/{ActId:int}")]

@attribute [Authorize(Roles = "Organization")]

<div class="container px-0 layout">
    <Title Name =@actName></Title>
    <Listcard  ActId =@ActId/>
</div>

@code {
   
}
