@layout AdminLayout

@attribute [Route(Routes.AdminEditOrganizationProfile + "/{UserId}")]
@attribute [Authorize(Roles = "Admin")]

<VMS.Pages.Organization.Profile.EditOrganizationProfile UserId="@UserId" IsUsedForAdmin="true"/>

@code {
    [Parameter] public string UserId { get; set; }
}
